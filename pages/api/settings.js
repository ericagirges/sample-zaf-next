// insert settings for requests here
// sample is of a GET request to retrieve all tickets
const settings = {
    url:'/api/v2/tickets.json',
    type:'GET',
    dataType: 'json'
};

export default settings;